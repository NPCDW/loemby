# 已知问题

## mpv 播放过程中突然结束并跳到下一集

### 触发条件
当播放服务器关闭或遭到攻击，导致 mpv 无法请求到数据流时发生。

### 行为描述
mpv 会尝试多次重试请求，如果仍然失败，则判定文件播放结束。此时若开启了预取功能，mpv 会自动加载下一集内容。

### 暂时无法解决
理应设置 mpv 无限次重试，直到用户观看到缓存结尾处被暂停，或者不应认为文件结束，而是抛出错误。