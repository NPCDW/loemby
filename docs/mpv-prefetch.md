# mpv 预取功能

## 工作原理
当文件播放到末尾（或缓存到末尾）时，mpv 会自动提前加载下一个文件。

## 存在的问题

### 字幕缓存问题
mpv 不会将内置字幕加载到缓存中。当切换到另一个内置字幕时，mpv 会丢弃所有缓存并重新加载文件。（注意：切换外置字幕时不会触发此问题）

### 预取机制的局限性
预取功能无法提前选择字幕，默认读取当前文件的字幕位置，并加载下一个文件相同位置的字幕。但如果下一个文件的首选字幕位置发生变化，预取的缓存将全部失效。